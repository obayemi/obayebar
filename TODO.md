- [x] app launcher
- [ ] iddle inhibitor thing
- [x] clic on notif focuses the app
- [x] faster launcher run
- [x] launcher navigation with arrows scrolls the viewport
- [x] resize the notification pannel dynamically according to the current number of notifications (or fix the thing). also do implement a limit to the number of notification that can be displayed, if the notification should take more than 2/5 of the screen, show a message "<n> other notifications" instead
- [x] gitlab todos module (enable with `--gitlab` or `[gitlab].enable` in `$XDG_CONFIG_HOME/obayebar/config.toml`; host via `--gitlab-url` / `OBAYEBAR_GITLAB_URL` / `[gitlab].url`; token via `OBAYEBAR_GITLAB_TOKEN`, Secret Service keyring, or `$XDG_CONFIG_HOME/obayebar/gitlab_token`. Home-manager exposes `programs.obayebar.gitlab.{enable,url,tokenFile}`.)
- [ ] wallpaper daemon (`obayebar-wallpaper`, replaces the `hyprwallp` fish script). Selection core, `[wallpaper]` config and the `wallpapers.json` state file are in `obayebar-core`; the wlr-layer-shell renderer is the remaining piece. Not hyprpaper — it leaks ~4MB per IPC request with no `unload` verb since 0.8.4.
- [ ] lock screen (`obayebar-lock`, replaces the `hyprrandlock` fish script). Generates a per-monitor hyprlock config from the wallpaper state so the lock shows what the desktop shows, blurred. Not a native ext-session-lock client: sessionlockev discards the `locked`/`finished` events and `.expect()`s on the unlock path, so a crashed locker means a permanently locked machine.
